# Docs Folder Example

This workspace indexes Markdown and text documentation.

```sh
cd examples/docs-folder
treedb-project-memory doctor
treedb-project-memory index --dry-run --json
```

The source entry is `docs`.
