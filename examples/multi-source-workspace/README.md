# Multi-Source Workspace Example

This workspace combines a repo source, docs folder, and JSONL export.

```sh
cd examples/multi-source-workspace
treedb-project-memory doctor
treedb-project-memory index --dry-run
```

The JSONL source uses the `body` field as chunk content.
