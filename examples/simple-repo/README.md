# Simple Repo Example

This workspace indexes a small source tree with Markdown and Python files.

```sh
cd examples/simple-repo
treedb-project-memory doctor
treedb-project-memory index --dry-run
```

The source entry is `app` and uses the `repo` type.
